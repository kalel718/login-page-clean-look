<!-- All our custom styling goes here to make the form look great. -->
  <style>
    /* We're importing the Poppins font from Google Fonts to give our text a modern look. */
    @import url(https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap");
    
    /* The universal selector (*) targets every element on the page. */
    /* We reset default browser margins and paddings, and tell elements to include padding/border in their size. */
    /* This makes layout much more predictable across different browsers. */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Poppins", sans-serif; /* Applies our chosen font to everything. */
    }

    /* Styles for the entire page body. */
    body{
      display: flex; /* Uses Flexbox to easily center content. */
      justify-content: center; /* Horizontally centers the content. */
      align-items: center; /* Vertically centers the content. */
      min-height: 100vh; /* Ensures the body takes up at least the full viewport height. */
      background: rgba(76,75,99); /* Sets a solid background color for the page. */
    }

    /* Styles for the main container that wraps our login form. */
    .wrapper{
      width: 420px; /* Sets a fixed width for the login form container. */
      background: transparent; /* Makes the background of the wrapper transparent. */
      border: 2px solid rgba(255, 255, 255, .2); /* Adds a subtle, semi-transparent white border. */
      backdrop-filter: blur(20px); /* This creates a cool frosted glass effect on anything behind it. */
      box-shadow: 0 0 10px rgba(0,0,0, .2); /* Adds a soft shadow for depth. */
      color: #fff; /* Sets the default text color inside the wrapper to white. */
      border-radius: 10px; /* Rounds the corners of the wrapper. */
      padding: 30px 40px; /* Adds space inside the wrapper, around its content. */
    }

    /* Styles for the main "Login" heading. */
    .wrapper h1{
      text-align: center; /* Centers the text horizontally. */
      font-size: 36px; /* Makes the font size quite large. */
    }

    /* Styles for each input field container (e.g., username, password). */
    .wrapper .input-box{
      width: 100%; /* Makes the input box take up the full width of its parent. */
      position: relative; /* Essential for positioning the icon inside the input field. */
      height: 50px; /* Sets a fixed height for the input box. */
      margin: 30px 0; /* Adds vertical space between input boxes. */
    }

    /* Styles for the actual input fields (username and password). */
    .input-box input{
      width: 100%; /* Makes the input field take up the full width of its container. */
      height: 100%; /* Makes the input field take up the full height of its container. */
      background: transparent; /* Makes the input field's background transparent. */
      border: none; /* Removes the default input border. */
      outline: none; /* Removes the default outline that appears on focus. */
      border: 2px solid rgba(255,255,255, .2); /* Adds a custom semi-transparent white border. */
      border-radius: 40px; /* Makes the input field very rounded. */
      font-size: 16px; /* Sets the font size for text typed inside. */
      color: #fff; /* Sets the text color inside the input field to white. */
      padding: 20px 45px 20px 20px; /* Adds internal spacing, making room for the icon on the right. */
    }

    /* Styles for the placeholder text inside the input fields. */
    .input-box input::placeholder {
      color: #fff; /* Makes the placeholder text white. */
    }

    /* Styles for the icons inside the input fields (user and lock icons). */
    .input-box i {
      position: absolute; /* Positions the icon absolutely within its parent (.input-box). */
      right: 20px; /* Places the icon 20px from the right edge. */
      top: 50%; /* Moves the icon's top edge to the vertical center. */
      transform: translateY(-50%); /* Shifts the icon up by half its own height to truly center it vertically. */
      font-size: 20px; /* Sets the size of the icon. */
      color: #fff; /* Sets the icon color to white. */
    }

    /* Styles for the "Remember me" checkbox and "Forgot password?" link container. */
    .wrapper .remember-forgot{
      display: flex; /* Uses Flexbox to arrange items horizontally. */
      justify-content: space-between; /* Distributes space evenly between the two items. */
      font-size: 14.5px; /* Sets the font size for this section. */
      margin: -15px 0 15px; /* Adjusts vertical spacing, pulling it up slightly. */
    }

    /* Styles for the checkbox input itself within the "Remember me" label. */
    .remember-forgot label input{
      accent-color: #fff; /* Changes the color of the checkbox itself (if supported by browser). */
      margin-right: 3px; /* Adds a small space between the checkbox and its text. */
    }

    /* Styles for the "Forgot password?" link. */
    .remember-forgot a {
      color: #fff; /* Sets the link color to white. */
      text-decoration: none; /* Removes the default underline from the link. */
    }

    /* Styles for the "Forgot password?" link when the mouse hovers over it. */
    .remember-forgot a:hover {
      text-decoration: underline; /* Adds an underline when hovered for visual feedback. */
    }

    /* Styles for the main "Login" button. */
    .wrapper .btn {
      width: 100%; /* Makes the button take up the full width. */
      height: 45px; /* Sets a fixed height for the button. */
      background: #fff; /* Sets the button's background color to white. */
      border: none; /* Removes the default button border. */
      outline: none; /* Removes the default outline on focus. */
      border-radius: 40px; /* Makes the button very rounded. */
      box-shadow: 0 0 10px rgba(0, 0, 0, .1); /* Adds a subtle shadow. */
      cursor: pointer; /* Changes the cursor to a pointer when hovering, indicating it's clickable. */
      color: #333; /* Sets the text color of the button to dark gray. */
      font-weight: 600; /* Makes the button text bold. */
    }

    /* Styles for the "Don't have an account? Register" link container. */
    .wrapper .register-link {
      font-size: 14.px; /* Sets the font size for this section. */
      text-align: center; /* Centers the text horizontally. */
      margin: 20px 0 15px; /* Adds vertical spacing around this section. */
    }

    /* Styles for the "Register" link within the register-link paragraph. */
    .register-link p a {
      color: #fff; /* Sets the link color to white. */
      text-decoration: none; /* Removes the default underline. */
      font-weight: 600; /* Makes the link text bold. */
    }

    /* Styles for the "Register" link when the mouse hovers over it. */
    .register-link p a:hover {
      text-decoration: underline; /* Adds an underline on hover. */
    }
  </style>
